version: 2.1
orbs:
  node: circleci/node@2.0.3
jobs:
  install-build-test:
    docker:
      - image: "cimg/base:stable"
    steps:
      - checkout
      - node/install:
          install-yarn: true
          node-version: latest
      - run: node --version
      - checkout
      - setup_remote_docker
      - run: npm run build && npm run test

workflows:
  build-and-test:
    jobs:
      - install-build-test
