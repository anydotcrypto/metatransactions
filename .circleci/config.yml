version: 2.1
orbs:
  node: circleci/node@2.0.3
jobs:
  build-and-test:
    executor:
      name: node/default
    steps:
      - checkout
      - setup_remote_docker
      - node/install-packages
      - run: npm run test
workflows:
  build-and-test:
    jobs:
      - build-and-test
